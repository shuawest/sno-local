---


- name: Enable UTM Rosetta x86_64 emulation
  hosts: client
  gather_facts: no
  tasks:

  - name: Enable Rosetta
    ansible.builtin.include_role:
      name: utm
      tasks_from: enable_rosetta.yml
